-- xSoftwares Skrypt Roman GameHUB - BloxStrike Edition
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "xSoftwares | Skrypt Roman GameHUB",
    LoadingTitle = "xSoftwares",
    LoadingSubtitle = "Roman GameHUB - BloxStrike",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "xSoftwaresRoman",
        FileName = "BloxStrikeConfig"
    },
})

local BloxStrikeTab = Window:CreateTab("BloxStrike", 4483362458)

BloxStrikeTab:CreateSection("Advanced BloxStrike Cheats")

-- ==================== VARIABLES ====================
local ESPEnabled = false
local ESPObjects = {}
local AimlockEnabled = false
local AimlockKey = Enum.UserInputType.MouseButton2  -- PPM
local AimlockFOV = 120
local AimlockSmooth = 8
local FOVCircle

-- ==================== STRONG ESP ====================
local function CreateESP(player)
    if player == game.Players.LocalPlayer then return end
    
    local function UpdateESP()
        if not ESPEnabled or not player.Character then 
            if ESPObjects[player] then
                for _, obj in pairs(ESPObjects[player]) do obj:Remove() end
                ESPObjects[player] = nil
            end
            return 
        end
        
        local character = player.Character
        local root = character:FindFirstChild("HumanoidRootPart")
        local head = character:FindFirstChild("Head")
        local humanoid = character:FindFirstChild("Humanoid")
        
        if not root or not head then return end
        
        -- Box + Name + Distance + Health
        if not ESPObjects[player] then
            ESPObjects[player] = {}
        end
        
        -- Box
        local box = ESPObjects[player].Box or Drawing.new("Square")
        box.Thickness = 2
        box.Color = Color3.fromRGB(255, 0, 0)
        box.Transparency = 1
        box.Filled = false
        ESPObjects[player].Box = box
        
        -- Name
        local name = ESPObjects[player].Name or Drawing.new("Text")
        name.Size = 16
        name.Color = Color3.fromRGB(255, 255, 255)
        name.Outline = true
        name.Center = true
        ESPObjects[player].Name = name
        
        -- Distance
        local distText = ESPObjects[player].Dist or Drawing.new("Text")
        distText.Size = 14
        distText.Color = Color3.fromRGB(200, 200, 200)
        distText.Outline = true
        distText.Center = true
        ESPObjects[player].Dist = distText
    end
    
    game:GetService("RunService").RenderStepped:Connect(UpdateESP)
end

BloxStrikeTab:CreateToggle({
    Name = "ESP Players (Box + Name + Distance)",
    CurrentValue = false,
    Flag = "StrongESP",
    Callback = function(Value)
        ESPEnabled = Value
        if Value then
            for _, player in pairs(game.Players:GetPlayers()) do
                CreateESP(player)
            end
            game.Players.PlayerAdded:Connect(CreateESP)
        else
            for _, objs in pairs(ESPObjects) do
                for _, obj in pairs(objs) do obj:Remove() end
            end
            ESPObjects = {}
        end
    end
})

-- ==================== EXCELLENT AIMLOCK ====================
local function CreateFOVCircle()
    if FOVCircle then FOVCircle:Remove() end
    FOVCircle = Drawing.new("Circle")
    FOVCircle.Thickness = 2
    FOVCircle.Color = Color3.fromRGB(255, 255, 255)
    FOVCircle.Transparency = 0.7
    FOVCircle.Filled = false
    FOVCircle.NumSides = 64
end

BloxStrikeTab:CreateToggle({
    Name = "Aimlock na Głowę (Bind: PPM)",
    CurrentValue = false,
    Flag = "Aimlock",
    Callback = function(Value)
        AimlockEnabled = Value
        if Value and not FOVCircle then CreateFOVCircle() end
        if not Value and FOVCircle then FOVCircle:Remove() end
    end
})

BloxStrikeTab:CreateSlider({
    Name = "Aimlock FOV",
    Range = {30, 500},
    Increment = 5,
    CurrentValue = 120,
    Flag = "AimFOV",
    Callback = function(Value)
        AimlockFOV = Value
        if FOVCircle then FOVCircle.Radius = Value end
    end
})

BloxStrikeTab:CreateSlider({
    Name = "Aimlock Smoothness",
    Range = {1, 30},
    Increment = 1,
    CurrentValue = 8,
    Flag = "AimSmooth",
    Callback = function(Value)
        AimlockSmooth = Value
    end
})

-- Aimlock Loop
game:GetService("RunService").RenderStepped:Connect(function()
    if FOVCircle then
        FOVCircle.Position = Vector2.new(game:GetService("UserInputService"):GetMouseLocation().X, game:GetService("UserInputService"):GetMouseLocation().Y)
        FOVCircle.Visible = AimlockEnabled
    end
    
    if not AimlockEnabled then return end
    
    local mouse = game.Players.LocalPlayer:GetMouse()
    local camera = workspace.CurrentCamera
    local closestDist = AimlockFOV
    local closestPlayer = nil
    
    for _, player in pairs(game.Players:GetPlayers()) do
        if player ~= game.Players.LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local head = player.Character.Head
            local screenPos, onScreen = camera:WorldToViewportPoint(head.Position)
            
            if onScreen then
                local mousePos = Vector2.new(mouse.X, mouse.Y)
                local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                
                if dist < closestDist then
                    closestDist = dist
                    closestPlayer = player
                end
            end
        end
    end
    
    if closestPlayer and closestPlayer.Character and closestPlayer.Character:FindFirstChild("Head") then
        local targetHead = closestPlayer.Character.Head.Position
        camera.CFrame = camera.CFrame:Lerp(
            CFrame.new(camera.CFrame.Position, targetHead), 
            1 / AimlockSmooth
        )
    end
end)

-- Bind (PPM)
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.UserInputType == AimlockKey then
        AimlockEnabled = true
    end
end)

game:GetService("UserInputService").InputEnded:Connect(function(input)
    if input.UserInputType == AimlockKey then
        AimlockEnabled = false
    end
end)

BloxStrikeTab:CreateSection("Info")
BloxStrikeTab:CreateLabel("Aimlock: Trzymaj PPM (Prawy przycisk myszy)")
BloxStrikeTab:CreateLabel("ESP jest bardzo widoczny i stabilny")

Rayfield:Notify({
    Title = "xSoftwares Roman GameHUB",
    Content = "BloxStrike Cheats załadowane - ESP + Aimlock gotowe!",
    Duration = 7,
    Image = 4483362458
})
